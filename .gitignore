node_modules/
.vercel/
.env*

# Local audit tool output (Fallow, Graphify). Regenerated on every scan.
.fallow/
graphify-out/

# gate-snapshot output (build artifact, never committed)
.build-websites-tools/

# transient probe directory used by the spaced-path test
tmp space probe/
